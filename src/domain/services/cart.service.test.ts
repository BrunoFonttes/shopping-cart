describe("tests cart service", () => {
	describe("tests addOrUpdateItem", () => {
		it("should return failure when item doesnt exist", () => {});
		it("should return failure when cartItem has validation errors", () => {});
		it("should return failure when its not possible to create/update cart", () => {});
		it("should return success", () => {});
	});

	describe("tests removeItem", () => {
		it("should return failure when item doesnt exist", () => {});
		it("should return failure when its not possible to create/update cart", () => {});
		it("should return success", () => {});
	});

	describe("tests getCart", () => {
		it("should return failure when its not possible to get cart", () => {});
		it("should return success", () => {});
	});
});
